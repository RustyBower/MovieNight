# Generate a random secret: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-to-a-random-string

# Optional: Plex app client identifier (a fixed UUID is fine)
PLEX_APP_CLIENT_ID=movienight-web-app

# Optional: Base URL for OAuth redirect (auto-detected from request if blank)
# BASE_URL=http://localhost:8000
